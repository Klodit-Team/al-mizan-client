FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
# Source, public, and next.config are volume-mounted from docker-compose.dev.yml
EXPOSE 3000
ENV NODE_ENV=development
CMD ["npm", "run", "dev"]
